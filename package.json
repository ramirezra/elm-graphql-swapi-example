{
  "name": "elm-graphql-swapi-example",
  "version": "1.0.0",
  
  "description": "Refactor of dillonkearn&#39;s graphqelm Ellie example using the Star Wars API",
  
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -r dist && parcel build src/index.html --public-url ./",
    "test": "elm-test",
    "autotest": "elm-test --watch"
  },
  
  "author": "Robinson Ramirez",
  
  
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-hot": "^1.0.1",
    "elm-test": "^0.19.0-rev6",
    "node-elm-compiler": "^5.0.3",
    "parcel-bundler": "^1.12.0"
  }
}
